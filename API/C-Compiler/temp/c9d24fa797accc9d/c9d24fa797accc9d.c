#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    while (n!=0){
        c++;
    if(n>0)
    printf("Positive numbers:%d",n);
    else if(n>0)
    printf("Negative numbers:%d",n);
    }
    return 0;
}
