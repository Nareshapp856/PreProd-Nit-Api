#include <stdio.h>
int main()
{
    int num=0;
    printf("enter the number");
    scanf("%d",&num);
    sum=0;
    intnum=sum;
    while(num!=0)
    sum=(sum+^(num%10),3);
    num=num/10;
    if(num==sum){
        printf("it is armstrong number");
    }
    else{
        printf("it is not armstrong number");
    }

}