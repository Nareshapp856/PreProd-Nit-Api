#include<stdio.h>
int main(){


long n, sqr,r,count,1;
printf("No: ");
scanf("%ld",&n);

 for(i=1;n!=0;n=n/10){
    r=r%10;
    count++;
    for(j=1; j<=count; j++){
        int div=div*10;
    }
    if(sqr%div==0)printf("%ld is an automorphic number.",n);
    else printf("%ld is not an automorphic number.",n);
 }
 return 0;








}