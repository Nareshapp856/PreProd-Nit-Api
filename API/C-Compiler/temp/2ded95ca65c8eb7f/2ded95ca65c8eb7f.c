#include<stdio.h>
int()
{
    int n,squere,temp=0,r;
    printf("enter the number");scanf("%d",&n);
    n=temp;
    squere=n*n;
    r=n/10;
    if(temp==r)print("%d is an automarphic number",r);
    else printf(this is not automarphic);
    
}