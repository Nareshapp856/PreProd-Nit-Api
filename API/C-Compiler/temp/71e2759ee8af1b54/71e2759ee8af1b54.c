#include<stdio.h>
int main(){
    int n,d,c,f;
    scanf("%d",&n);
    if(n%10=f)
    c++;
    {
printf("%d is an atomorphic number",f);}
else while()
n=f%10;
{
    printf("%d is an atomorphic number",f);
    return 0;
}
}