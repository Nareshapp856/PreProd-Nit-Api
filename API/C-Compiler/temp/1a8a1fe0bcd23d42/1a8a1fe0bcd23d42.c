#include<stdio.h>
int main(){
    int number;
    scanf("%d"&number);
    while(n>=0){
        if(n>=0)
        printf("Positive numbers:%d",number);
        else
        printf("Negative numbers:%d",number);
    }
    return 0;
}