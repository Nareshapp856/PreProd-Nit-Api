#include<stdio.h>
int main()
{
   int n;
   //printf("Enter a number");
   scanf("%d",&n);
   while(n!=0)
   {
    r=n%10;
    rev=rev*r+10;
    n=n/10;
   }
  if() printf("is an automorphic number.");
  else
  printf("is not an automorphic number.
")



}