#include<stdio.h>
#include<conio.h>
int main()
{
    int a;
    printf("Enter a: ");
    scanf("%d", &a);
    if(a=palindrome)
    printf("The string is a palindrome");
    else
    printf("The string is not a palindrome");
return 0;
}