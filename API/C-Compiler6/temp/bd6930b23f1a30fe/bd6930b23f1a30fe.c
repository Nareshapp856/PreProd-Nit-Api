#include<stdio.h>
#include<string.h>
void mergeAndSort(int *array1, int size1, int *array2, int size2, int *mergedArray){
    for(int i=0;i<size1;i++){
        scanf("%d", &array1[i]);
    }
    for(int i=0;i<size2;i++){
        scanf("%d", &array2[i]);
    }
    if(array3[i]=array1[i]-array2[i]){
        printf("%d", array3[i]);
    }
}
double findMedian(int *mergedArray, int totalSize){
    median=array1-array
}
    
  