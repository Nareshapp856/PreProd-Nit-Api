#include<stdio.h>
#include<conio.h>
int main()
{
    int a, b, palindrome;
    if(a = palindrome)
    printf("The string is a palindrome");
    else(b = not a palindrome)
    printf("The string is not a palindrome");
return 0;
}