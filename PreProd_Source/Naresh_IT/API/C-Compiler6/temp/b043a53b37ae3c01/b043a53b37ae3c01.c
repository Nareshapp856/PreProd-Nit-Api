#include<stdio.h>
int main()
{
    char s[100],i;
    printf("enter the string");
    scanf("%s",&s);
    for(i=0;s[i]!='\0';i++)
    if(while(i))
    for(i=0;i<1;--i)
    {
        printf("%s",s);
    }
    return 0;
}