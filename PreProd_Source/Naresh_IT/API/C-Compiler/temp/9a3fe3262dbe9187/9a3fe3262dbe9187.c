#include<stdio.h>
int main()
{
    int first number,second number;
    scanf("%d%d",&first number,&second number);
    int add,sub,mult,divi;
    add=first number + second number;
    sub =first number -second number;
    mult =first number * second number;
    divi=first number / second number;
    printf("%d%d",+,-,*,/);
}