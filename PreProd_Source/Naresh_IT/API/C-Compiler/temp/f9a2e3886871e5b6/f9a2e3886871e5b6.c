#include<stdio.h>
int main(){
    char number;
    scanf("%d"&number);
    while(n>=0){
        if(n>=0)
        printf("Positive numbers:%c",number);
        else
        printf("Negative numbers:%c",number);
    }
    return 0;
}