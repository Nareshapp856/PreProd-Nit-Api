#include<stdio.h>
int main()
{
    int a,b,operator;
    printf("enter a operator(+,-,*,/)");
    scanf("%f",&operator);
    printf("Enter first number :");
    scanf("%f",&a);
    printf("Enter second number :");
    scanff("%f",&b);
    if(operator==+)?printf("Addition is :"a+b):(operator==-)?printf("Subtraction is:"a-b):(operator==*)?printf("multiplication:",a*b):(operator==/)?printf("Division is:",a/b):Invalid Operator
    {
        printf("Addition is :"a+b);
    }
    else()

}