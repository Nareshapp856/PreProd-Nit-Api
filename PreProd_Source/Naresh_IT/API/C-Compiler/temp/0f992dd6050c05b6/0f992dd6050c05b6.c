#include<stdio.h>
int main() {
    int a=10,b=200,c=134;
    printf("enter a three digit ");
    scanf("%d%d%d", &a,&b,&c);
    (a>bb&&a<c)?printf("max=%d",a):(b>c &&>a)?prnintf("max=%d",b):(c>a&&c>b)?printf("max=%d,c"):printf(all numbers are same");
    return 0;
}