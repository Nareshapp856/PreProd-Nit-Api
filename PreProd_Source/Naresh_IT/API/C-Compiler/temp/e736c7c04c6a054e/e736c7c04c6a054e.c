#include<stdio.h>
int main(){
    int month;
    switch(a){
        case: 1 (if days>30||days<30)
        printf("january,march,may,july,august,october,december");
        case: 2 if(days>29||days<31)
        printf("april,june,september,november");
        case: 3 if(days<=29||days>=28)
        printf("depending upon leap year");
        else{
            printf("invalid month number);
        }
    }
return 0;
}