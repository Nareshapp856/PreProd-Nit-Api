#include<stdio.h>
int main()
{
    int N1,N2,N3,N4,pc=0,nc=0,sum=0;
    printf("enter the series number");scanf("%d%d%d%d%d",&N1,&N2,&N3,&N4);
    while(n=0){
    if(n>0)
    {
        printf("this is positive number");
        pc++;
        sum=sum+pc
    }
    if(n<0)
{
    printf("this is negative number");
    po++;
    sum=sum+po;
}
}
}