#include<stdio.h>
int main() {
    int ch;
    int a,b;
    scanf(%c, &ch);
    scanf(%f, &a);
    scanf(%f, &b);
    ch == '+' && %.2f(a+b) || '-' == %.2f(a-b) || '*' == %.2f(a*b) || '/' == %.2f(a/b);
    return 0;
}