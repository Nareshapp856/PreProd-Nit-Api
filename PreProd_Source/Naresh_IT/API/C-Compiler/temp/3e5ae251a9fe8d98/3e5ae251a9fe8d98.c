#include<stdio.h>
int main(){
    int n,a,f;
    scanf("%d",choice);
        switch(n){
            case 25:a=n%10;
            f=a*a;
            printf("%d is an automorphic number.",f);break;
            case 625:a=n%10;
        }
        return 0;
    }