#include<stdio.h>
int main()
{
    int n i,c;
    printf("enter a number");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    
        while(n!=0)
        {
            if(n%i==0)
            {
                if(c==2)
           
            printf("%d is a prime number.");
            else
            printf(" is not a prime number");

        }

    }
}
