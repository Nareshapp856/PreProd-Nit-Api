include<stdio.h>
int main(){
    int n,a,f;
    scanf("%d",&n);
    while(n){
        a=n%10;
       f=a*a; 
    }
    printf("%d is an automorphic number",n);
include<stdio.h>
int main(){
    int n,a,f;
    scanf("%d",&n);
    while(n){
        a=n%10;
       f=a*a; 
    }
    printf("%d is an automorphic number",f);
}