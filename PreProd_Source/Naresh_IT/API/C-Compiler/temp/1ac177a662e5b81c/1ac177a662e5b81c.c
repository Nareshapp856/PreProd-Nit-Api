# include<stdio.h>
int main()
{
    //area ofa square
    int side;
     int square=side*side;
     scanf("%d\nSide of the square",&side);
    printf("%d\nThe area of the square with side 5 is:",square);
    // area of rectangle
    int length;
    int breath;
    int rectangle=length*breath;
    scanf("%d\nLength and breadth of the rectangle are:",recangle)
    printf("%d\nThe area of the rectangle with length 4 and breadth 5 is:",&rectangle);

}