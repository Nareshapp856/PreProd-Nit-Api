#include<stdio.h>
int main(){
    int choice,a,f;
    scanf("%d",choice);
        switch(choice){
            case 25:a=n%10;
            f=a*a;
            printf("%d is an automorphic number.",f);break;
")
        }
    }