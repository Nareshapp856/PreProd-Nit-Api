#include<stdio.h>
int main()
 {
 int a,b,c;
    scanf("%d%d%d", &a,&b,&);
    (a>b&&a<c)? printf("max=%d",a):(b>c&&>a)?printf("max=%d",b):(c>a&&c>b)? printf("max=%d",c):printf(all numbers are same");
    return 0;
}